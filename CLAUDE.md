# カイトクラフト

- ゲーム本体は `index.html` 1ファイル。`main` に入るとGitHub Pagesへ自動で公開される。

## バージョン表示

- トップ画面の `<p id="ver">ver X.Y.Z</p>`（`index.html`）にアプリのバージョンを表示している。
- `main` へ反映するときは、そのPRに必ずバージョンの更新を含める。
  - 新しい機能（キャラ・武器・ステージなど）を含む → Y を上げて Z を0に（例：1.7.0 → 1.8.0）
  - バグ修正や調整だけ → Z を上げる（例：1.7.0 → 1.7.1）
- 反映の報告では、新しいバージョン番号をユーザーに伝える。
